import styled from 'styled-components'

import { cores } from '../../../styles'

export const Container = styled.footer`
  background-color: ${cores.white};
  padding: 40px 0;
  text-align: center;
`

export const LogoFotter = styled.div`
  padding-bottom: 32px;
`

export const SocialMedia = styled.div`
  padding-bottom: 80px;
`

export const DescricaoFoot = styled.div`
  font-size: 10px;
  color: ${cores.pink};
`
